{
  "name": "filter-fyi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute filter-fyi-waitlist --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute filter-fyi-waitlist --remote --file=./schema.sql",
    "db:list:local": "wrangler d1 execute filter-fyi-waitlist --local --command='SELECT id, email, source, created_at FROM waitlist ORDER BY created_at DESC LIMIT 20'",
    "db:list:remote": "wrangler d1 execute filter-fyi-waitlist --remote --command='SELECT id, email, source, created_at FROM waitlist ORDER BY created_at DESC LIMIT 20'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
